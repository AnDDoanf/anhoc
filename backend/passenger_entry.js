// passenger_entry.js
// This script simply loads the TypeScript server.
// For cPanel, make sure you configure the environment variable NODE_OPTIONS to '--import tsx'
// so that Node.js can automatically compile and run the TypeScript files.
import './server.ts';
